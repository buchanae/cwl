cwlVersion: v1.0
class: CommandLineTool
baseCommand: echo
inputs:
  - id: message
    type: string
  #message: #string
    #type: string
    #inputBinding:
      #position: 1

# comment
outputs:
  - id: foo
    outputSource: bar
